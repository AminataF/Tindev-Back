{% extends 'base.html.twig' %}

{% block title %}Availability{% endblock %}

{% block body %}
    <h1>Availability</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ availability.id }}</td>
            </tr>
            <tr>
                <th>Availability</th>
                <td>{{ availability.availability }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_availability_index') }}">back to list</a>

    <a href="{{ path('app_backoffice_availability_edit', {'id': availability.id}) }}">edit</a>

    {{ include('backoffice/availability/_delete_form.html.twig') }}
{% endblock %}
